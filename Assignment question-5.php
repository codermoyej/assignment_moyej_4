<?php

// 5.Write a PHP function to find the second largest number in an array of numbers.
include_once("functions.php");

$allNumbers = array(44, 119, 10, 2, 5, 2, 99, 0, 31, 55);
print_r(secondLargestNumber($allNumbers));
